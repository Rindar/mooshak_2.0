@model IEnumerable<mooshak_2._0.Models.ApplicationUser>
@{
    ViewBag.Title = "UserList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center">All Registered Users</h2>
@Html.ActionLink("Create New User", "Register", "Account", null, new { @class = "btn btn-info" })

<table class="table table-bordered table-condensed">
<thead>
    <tr>
        <th style="text-align: center;">User Name</th>
        <th style="text-align: center;">Delete</th>
    </tr>
</thead>
<tbody>
    @foreach (var item in Model)
            {
        <tr>
            <td style="text-align: center;">@Html.DisplayFor(modelItem => item.Email)</td>
            <td style="text-align: center;"><a class="pointer" onclick="deleteUser('@Html.DisplayFor(modelItem => item.Id)')">Delete</a></td>
        </tr>
    }
</tbody>


    </table>

<script>
    
    function deleteUser(id)
    {
    var toDelete = confirm("Are you sure you want to delete this User?");
        if(toDelete == true)
        {
            window.location.assign("/Admin/DeleteUser/"+ Id);
        }
    }

</script>
<style type="text/css">
    .pointer:hover {
        cursor: pointer;
    }
</style>




