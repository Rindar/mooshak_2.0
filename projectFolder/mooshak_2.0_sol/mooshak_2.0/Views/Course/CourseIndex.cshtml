@model IEnumerable<mooshak_2._0.Models.ViewModels.CourseViewModel>
@{
    ViewBag.Title = "CourseIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="list-group">


    @using (Html.BeginForm())
    {
        <span>Add a new Course: </span>
        <input type="text" name="new_item" /><br />
        <input type="submit" value="Add Item" />
    }
    <br />

    <table class="table table-bordered table-condensed">
        <thead>
            <tr>
                <th style="text-align: center;">Course Name</th>
                <th style="text-align: center;">Edit</th>
                <th style="text-align: center;">Remove</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="text-align: center;">@Html.DisplayFor(modelItem => item.Title)</td>
                    <td style="text-align: center;"><a href="@Url.Action("Edit","Home")/@Html.DisplayFor(modelItem => item.ID)">Edit</a></td>
                    <td style="text-align: center;"><a class="pointer-arrow" onclick="deleteRow(@Html.DisplayFor(modelItem => item.ID))">Delete</a></td>
                </tr>
            }
        </tbody>


    </table>
</div>

<script>
    function deleteRow(id)
    {
        var toDelete = confirm("Are you sure you want to delete this course?")
        if(toDelete ==true)
        {
            window.location.assign("/Course/Delete/" + id);
        }

    }
</script>
<style type="text/css">
    .pointer-arrow:hover
    {
        cursor:pointer;
    }
</style>